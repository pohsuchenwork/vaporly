## Summary

<!-- What does this change and why? -->

## Testing

<!-- How was this verified? (commands run, manual steps) -->

## Checklist

- [ ] `bun run lint` and `bun run build` pass
- [ ] `cargo test` and `cargo clippy` pass (src-tauri)
- [ ] `bun run check:translations` passes if locale files changed
